spring:
  profiles: it
  application:
    name: stocks
  datasource:
    url: jdbc:h2:mem:test;
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true

liquibase:
  enabled: false