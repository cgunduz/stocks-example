spring:
  profiles: it
  application:
    name: stocks
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true

liquibase:
  enabled: false